---
"screeps-client": patch
---

Render ramparts in their own layer above the room's ambient lighting overlay, so the glow reads at full brightness instead of being dimmed by the dark-overlay multiply — matching the reference client, which draws ramparts in its topmost "effects" layer past the light map.

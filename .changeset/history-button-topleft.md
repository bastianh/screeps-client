---
"screeps-client": patch
---

Move the room-view History toggle to the top-left below the World Map button, end history mode when switching rooms, and only show the button when the server advertises history support (`historyChunkSize` in `/api/version`).

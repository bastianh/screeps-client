---
"screeps-client": patch
---

Remove the `l`/`c`/`y` keyboard shortcuts for the log, console, and memory panels. The `c` shortcut clashed with copy/paste; the panels remain toggleable via their toolbar buttons.

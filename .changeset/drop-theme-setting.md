---
"screeps-client": patch
---

Remove the "Structure theme" setting. The default theme was the only complete one — the alternative, "Vector (procedural)", dropped every sprite-backed object down to a plain fallback — so the default becomes the only rendering path and the room view now looks the same for everyone.

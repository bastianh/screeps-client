---
"screeps-client": patch
---

Improve map room ownership visualization with distinct overlays for owned vs enemy-claimed rooms. Own rooms now display with a blue overlay, while enemy-owned rooms display with a red overlay. Also fixes map mode to display by default when loading without a room and ensures map zoom persists only when viewing a specific room.
